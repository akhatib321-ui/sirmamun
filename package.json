{
  "name": "sirmamun",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "api": "npm run start:dev --workspace=apps/api",
    "web": "npm run dev --workspace=apps/web"
  },
  "devDependencies": {
    "@swc/cli": "^0.5.0",
    "@swc/core": "^1.10.0"
  }
}
